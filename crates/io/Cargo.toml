[package]
name = "io"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
csv = "1.1"
formats = { path = "../formats" }
once_cell = "1.17"

[dev-dependencies]
tempfile = "3.6"
